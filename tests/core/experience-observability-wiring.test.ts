import test from 'node:test';import assert from 'node:assert/strict';import { readFileSync } from 'node:fs';const source=readFileSync(new URL('../../src/experience/ExperienceCanvas.tsx',import.meta.url),'utf8');test('ExperienceCanvas routes the sole R3F frame sample through observability integration',()=>{assert.match(source,/observeRuntimeFrame/);assert.match(source,/useObservability/);assert.doesNotMatch(source,/\bobserveFrame\(/);});
