import test from 'node:test';import assert from 'node:assert/strict';import { existsSync,readFileSync } from 'node:fs';const spec=new URL('../e2e/v2.1-observability.spec.ts',import.meta.url);test('V2.1 Playwright suite persists desktop/mobile diagnostics and covers privacy plus recovery',()=>{assert.equal(existsSync(spec),true);const source=readFileSync(spec,'utf8');assert.match(source,/v2\.1-diagnostics/);assert.match(source,/performance-diagnostics\.json/);assert.match(source,/__CDJ_TELEMETRY_OVERRIDE__/);assert.match(source,/globalPrivacyControl/);assert.match(source,/webglcontextlost/);assert.match(source,/webglcontextrestored/);assert.match(source,/turn sound on/i);});
