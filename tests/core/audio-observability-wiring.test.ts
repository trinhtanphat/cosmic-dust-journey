import test from 'node:test';import assert from 'node:assert/strict';import { readFileSync } from 'node:fs';const source=readFileSync(new URL('../../src/components/SoundToggle.tsx',import.meta.url),'utf8');test('SoundToggle observes audio lifecycle through observability without emitting envelope values',()=>{assert.match(source,/useObservability/);assert.match(source,/onStateChange/);assert.match(source,/audio\.enabled/);assert.match(source,/audio\.disabled/);assert.doesNotMatch(source,/lowHz|highHz|filterHz|noise/);});
