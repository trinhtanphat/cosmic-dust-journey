import test from 'node:test';import assert from 'node:assert/strict';import { readFileSync } from 'node:fs';const source=readFileSync(new URL('../../src/observability/react.tsx',import.meta.url),'utf8');const pkg=JSON.parse(readFileSync(new URL('../../package.json',import.meta.url),'utf8')) as {dependencies?:Record<string,string>};test('provider lazily attaches configured telemetry and pins the browser SDK dependency',()=>{assert.match(source,/createConfiguredAdapters/);assert.match(source,/readBrowserTelemetryConfig/);assert.equal(pkg.dependencies?.['@sentry/browser'],'^10.73.0');});
