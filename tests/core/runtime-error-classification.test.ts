import test from 'node:test';import assert from 'node:assert/strict';import { classifyRuntimeError } from '../../src/observability/errors.ts';test('runtime error classification returns only a bounded class and never the message',()=>{assert.equal(classifyRuntimeError(new TypeError('secret ?token=abc')),'TypeError');assert.equal(classifyRuntimeError('secret'),'StringRejection');assert.equal(classifyRuntimeError({}),'ObjectRejection');assert.equal(classifyRuntimeError(null),'NullRejection');});
