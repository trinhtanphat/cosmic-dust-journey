import test from 'node:test';import assert from 'node:assert/strict';import { readFileSync } from 'node:fs';const shell=readFileSync(new URL('../../src/app/ExperienceShell.tsx',import.meta.url),'utf8');const canvas=readFileSync(new URL('../../src/experience/ExperienceCanvas.tsx',import.meta.url),'utf8');test('renderer recovery is wired outside narrative DOM and context listeners live inside Canvas',()=>{assert.match(shell,/RuntimeErrorBoundary/);assert.match(shell,/reduceWebGLRecovery/);assert.match(canvas,/WebGLRecoveryBoundary/);assert.match(canvas,/onContextLost/);assert.match(canvas,/onContextRestored/);assert.match(shell,/setWebgl\(supported\)[\s\S]*?\}, \[observability\]\);/);});
